spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/weight-loss?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: :)

logging:
  level:
    top.okeng: DEBUG
    org.springframework: INFO
    org.springframework.boot.autoconfigure: INFO

debug: true