spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://${DB_HOST:mariadb}:${DB_PORT:3306}/${DB_NAME:weight-loss}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${DB_USER:root}
    password: ${DB_PWD:12345678}

logging:
  level:
    top.okeng: DEBUG
    org.springframework: INFO
    org.springframework.boot.autoconfigure: INFO

debug: true